{
  "name": "duckdb-quack-datasource",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/module.ts --bundle --outfile=dist/module.js --format=esm --sourcemap --external:react --external:react-dom --external:@grafana/*",
    "tsc": "tsc --noEmit"
  },
  "dependencies": {
    "@grafana/data": "12.1.1",
    "@grafana/runtime": "12.1.1",
    "@grafana/ui": "12.1.1",
    "react": "18.3.1",
    "rxjs": "^7.8.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.6.0"
  }
}
